# Required
GEMINI_API_KEY=AIza-YOUR_KEY_HERE

# Optional — override the URL reported in the agent card (useful when deployed behind a proxy)
# AGENT_URL=http://localhost:8004

# Optional — override the port the server binds to (default: 8004)
# PORT=8004
