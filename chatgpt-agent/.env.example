# Required
CHATGPT_API_KEY=sk-YOUR_KEY_HERE

# Optional — override the URL reported in the agent card (useful when deployed behind a proxy)
# AGENT_URL=http://localhost:8003

# Optional — override the port the server binds to (default: 8003)
# PORT=8003
